[project]
name = "pi2-rec"
version = "0.1.0"
requires-python = ">=3.10"

dependencies = [
  "sounddevice",
  "luma.oled",
  "Pillow",
  "requests",
  "python-dotenv",
  "pydub",
  "ffmpeg-python",
]

[tool.uv]
